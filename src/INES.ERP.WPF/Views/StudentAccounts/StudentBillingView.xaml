<Page x:Class="INES.ERP.WPF.Views.StudentAccounts.StudentBillingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:viewModels="clr-namespace:INES.ERP.WPF.ViewModels.StudentAccounts"
      Title="Student Billing">

    <Page.DataContext>
        <viewModels:StudentBillingViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="{StaticResource LargePadding}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Student Billing"
                               Style="{StaticResource HeadingTextStyle}"/>
                    <TextBlock Text="Manage student bills and fees"
                               Style="{StaticResource CaptionTextStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Command="{Binding CreateBillCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="{StaticResource SmallMargin}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="CREATE BILL"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding RefreshCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="{StaticResource SmallMargin}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="REFRESH"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding ExportCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="{StaticResource SmallMargin}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Export" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="EXPORT"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="{StaticResource MediumPadding}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CompactCardStyle}" Margin="{StaticResource SmallMargin}">
                <StackPanel>
                    <TextBlock Text="Total Bills" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding Bills.Count}" 
                               FontSize="{StaticResource FontSizeXLarge}"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CompactCardStyle}" Margin="{StaticResource SmallMargin}">
                <StackPanel>
                    <TextBlock Text="Total Outstanding" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding TotalOutstanding, StringFormat='{}{0:C0}'}" 
                               FontSize="{StaticResource FontSizeXLarge}"
                               FontWeight="Bold"
                               Foreground="{StaticResource WarningBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CompactCardStyle}" Margin="{StaticResource SmallMargin}">
                <StackPanel>
                    <TextBlock Text="Total Paid" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding TotalPaid, StringFormat='{}{0:C0}'}" 
                               FontSize="{StaticResource FontSizeXLarge}"
                               FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Border Grid.Row="2" Style="{StaticResource StandardCardStyle}" Margin="{StaticResource MediumPadding}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search Bar -->
                <Grid Grid.Row="0" Margin="{StaticResource MediumMargin}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Search by student name, ID, or bill type..."
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             MaxWidth="400"
                             HorizontalAlignment="Left"/>

                    <Button Grid.Column="1"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Margin="8,0,0,0">
                        <materialDesign:PackIcon Kind="Magnify" Width="20" Height="20"/>
                    </Button>
                </Grid>

                <!-- Bills DataGrid -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Bills}"
                          SelectedItem="{Binding SelectedBill}"
                          Style="{StaticResource StandardDataGridStyle}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Student ID" Binding="{Binding StudentId}" Width="100"/>
                        <DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="200"/>
                        <DataGridTextColumn Header="Bill Type" Binding="{Binding BillType}" Width="120"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat='{}{0:C0}'}" Width="100"/>
                        <DataGridTextColumn Header="Paid" Binding="{Binding PaidAmount, StringFormat='{}{0:C0}'}" Width="100"/>
                        <DataGridTextColumn Header="Outstanding" Binding="{Binding OutstandingAmount, StringFormat='{}{0:C0}'}" Width="120"/>
                        <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="100"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Command="{Binding DataContext.EditBillCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                ToolTip="Edit Bill">
                                            <materialDesign:PackIcon Kind="Edit" Width="16" Height="16"/>
                                        </Button>
                                        <Button Command="{Binding DataContext.DeleteBillCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                ToolTip="Delete Bill">
                                            <materialDesign:PackIcon Kind="Delete" Width="16" Height="16"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Width="50" Height="50"
                             IsIndeterminate="True"
                             Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="Loading bills..."
                           Foreground="White"
                           FontSize="{StaticResource FontSizeNormal}"
                           Margin="{StaticResource MediumMargin}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
